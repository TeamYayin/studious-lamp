<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <meta name="csrf-token" content="<%= csrfToken %>"/>
    <title><%= title %></title>
  </head>
  <body class="bg-slate-300 flex justify-center items-center h-screen">
    <div class="grid grid-cols-1 md:grid-cols-6">
        <div class="col-span-3 md:col-start-2 md:col-span-3 p-6 bg-gray-100 rounded-lg shadow-md">
            <h1 class="text-2xl font-bold text-gray-800">Welcome to Our Website</h1>
            <p class="text-gray-600 mt-3">New here? <a href="/signup" class="block bg-blue-500 hover:bg-blue-600 text-white py-2 px-20 rounded-lg font-bold">Sign up now</a></p>
            <p class="text-gray-600 mt-2">Already have an account? <a href="/login" class="block bg-blue-500 hover:bg-blue-600 text-white py-2 px-20 rounded-lg font-bold">Sign in now</a></p>
        </div>
    </div>
  </body>
</html>